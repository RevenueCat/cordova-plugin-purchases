import {
  PurchasesVirtualCurrencies,
  PurchasesVirtualCurrency
} from "../www/plugin";

function checkVirtualCurrencies(virtualCurrencies: PurchasesVirtualCurrencies) {
    const all: { [key: string]: PurchasesVirtualCurrency } = virtualCurrencies.all;
}
