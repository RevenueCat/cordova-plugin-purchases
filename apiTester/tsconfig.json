{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "target": "es2015",
    "types": ["node"],
    "rootDir": "..",
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    // We don't actually need to output anything
    "noEmit": true
  },
  "include": [
    "./*"
  ]
}
